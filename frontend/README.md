# F1 Dashboard

F1レースデータの可視化・分析ダッシュボード

## 技術スタック

- **フロントエンド**: React 18 + TypeScript
- **ルーティング**: React Router v7
- **ビルドツール**: Vite
- **可視化ライブラリ**: Recharts / D3.js
- **状態管理**: Zustand
- **データフェッチング**: TanStack Query (React Query)
- **スタイリング**: Tailwind CSS
- **HTTP クライアント**: Axios
- **テスト**: Vitest + React Testing Library
- **デプロイ**: Vercel

## セットアップ

### 依存関係のインストール

```bash
npm install
```

### 環境変数の設定

`.env.local` ファイルを作成（`.env.example` を参考）

```bash
cp .env.example .env.local
```

## 開発

### 開発サーバーの起動

```bash
npm run dev
```

ブラウザで http://localhost:3000 を開く

### ビルド

```bash
npm run build
```

### プレビュー

```bash
npm run preview
```

### テスト

```bash
# テストを実行
npm test

# テストをwatch モードで実行
npm test -- --watch

# カバレッジを表示
npm run test:coverage

# テストUIを起動
npm run test:ui
```

### リント・フォーマット

```bash
# Lintチェック
npm run lint

# コードフォーマット
npm run format
```

## プロジェクト構成

```
f1_dashboard/
├── docs/                    # 仕様書
│   └── specifications/
│       ├── 00_tech_stack.md
│       ├── 01_common_features.md
│       ├── 02_api_client.md
│       └── 03_race_lap_pit_analysis.md
├── public/                  # 静的ファイル
├── src/
│   ├── components/          # Reactコンポーネント
│   │   ├── common/
│   │   ├── charts/
│   │   ├── filters/
│   │   └── layouts/
│   ├── pages/               # ページコンポーネント
│   ├── lib/                 # ライブラリ・ユーティリティ
│   │   ├── api/             # API関連
│   │   │   ├── client.ts    # HTTPクライアント
│   │   │   ├── types.ts     # 型定義
│   │   │   └── f1/          # Open F1 API
│   │   ├── hooks/           # カスタムフック
│   │   └── utils/           # ユーティリティ関数
│   ├── store/               # Zustandストア
│   ├── test/                # テスト設定
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── vite-env.d.ts            # Vite型定義
├── vite.config.ts
├── vitest.config.ts
├── tailwind.config.js
├── tsconfig.json
└── package.json
```

## 実装状況

### ✅ 完了

- [x] プロジェクト初期化（Vite + React + TypeScript）
- [x] 技術スタック設定
- [x] API Client実装
  - [x] HTTPクライアント（Axios）
  - [x] 型定義（Open F1 API）
  - [x] API サービス層（sessions, meetings, drivers, laps, pit, stints, positions）
  - [x] React Query カスタムフック
  - [x] テスト（41 tests passed）
- [x] 共通機能
  - [x] セッション選択機能
  - [x] ドライバーフィルタリング機能
  - [x] URLパラメータ管理
  - [x] Zustand状態管理（お気に入り、UI状態）
  - [x] ユーティリティ関数（フォーマット、クラス名結合）
  - [x] 共通コンポーネント（Loading, ErrorMessage）

### 🚧 進行中

- [ ] 本戦分析
  - [ ] ラップタイム推移グラフ
  - [ ] ピット戦略タイムライン
  - [ ] タイヤデグラデーション分析
- [ ] 共通機能
  - [ ] データエクスポート機能

### 📋 計画中

- [ ] 予選分析機能
- [ ] レスポンシブデザイン
- [ ] 国際化（i18n）
- [ ] PWA対応

## API

このプロジェクトは[Open F1 API](https://openf1.org/)を使用しています。

- ベースURL: `https://api.openf1.org/v1`
- 認証: 不要
- レート制限: なし（適切な使用を推奨）

## デプロイ

Vercelへのデプロイ:

```bash
# Vercel CLIインストール
npm i -g vercel

# デプロイ
vercel

# 本番デプロイ
vercel --prod
```

## ライセンス

MIT

## 貢献

Issue、Pull Requestを歓迎します。

## ドキュメント

詳細な仕様書は `docs/specifications/` を参照してください。
